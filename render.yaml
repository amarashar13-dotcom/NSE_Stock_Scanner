services:
  - type: web
    name: nse-stock-scanner
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 2 --timeout 180 --graceful-timeout 30 -b 0.0.0.0:$PORT app:app
    healthCheckPath: /
    plan: free
